services:
  - name: health-predictor
    type: web
    git:
      branch: main
    build:
      buildpack:
        run_command: pip install -r requirements.txt
    run:
      command: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120
    ports:
      - port: 8000
        protocol: http